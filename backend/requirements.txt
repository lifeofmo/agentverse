fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
python-multipart==0.0.12

# Password hashing (slow/salted — safe for production)
bcrypt==4.2.1

# Async database layer (SQLite dev → PostgreSQL prod)
databases[aiosqlite,asyncpg]==0.9.0
aiosqlite==0.20.0
asyncpg==0.29.0

# Async task queue (Redis-backed)
arq==0.26.1

# Stripe payments
stripe>=9.0.0
